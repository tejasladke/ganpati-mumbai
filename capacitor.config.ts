import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ganpatimumbaiexplorer.app',
  appName: 'Ganpati Mumbai Explorer',
  webDir: 'dist'
};

export default config;
